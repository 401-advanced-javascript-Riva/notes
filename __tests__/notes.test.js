const Input = require('../lib/input.js');



describe('Testing if a command was given', () => {
test('If the command (add) and data (the note) were both valid, assert that the console shows the output.', () => {
expect()).toBeTruthy();
expect().toBeFalsy();
  });
});